<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bcsd.callvan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Intro -->
        <activity
            android:name=".Activiy.IntroActivity"
            android:theme="@style/AppTheme.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Main -->
<<<<<<< HEAD
        <activity android:name=".MainActivity">
        </activity>

        <!-- Login -->
        <activity android:name=".LoginActivity">
        </activity>

        <!-- Register -->
        <activity android:name=".RegisterActivity">
        </activity>

        <!--New Room-->
        <activity android:name=".CreateRoomActivity"/>

        <!--Wait Room-->
        <activity android:name=".WaitRoomActivity"/>

        <!--Gathering Room-->
        <activity android:name=".GatheringActivity"/>
=======
        <activity android:name=".Activiy.MainActivity" />
        <!-- Login -->
        <activity android:name=".Activiy.LoginActivity" />
        <!-- Register -->
        <activity android:name=".Activiy.RegisterActivity" />
        <!--
        <activity
            android:name=".Activiy.ProxyTestActivity"
            android:label="@string/title_activity_proxy_test"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> -->
>>>>>>> frontActivity
    </application>

</manifest>
